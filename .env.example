# =============================================================================
# CONFIGURACIÓN PARA DESPLIEGUE EN RENDER
# Copia este archivo como .env y configura las variables necesarias
# =============================================================================

# Configuración de Base de Datos (Render PostgreSQL)
# IMPORTANTE: Obtén esta URL desde el dashboard de Render -> PostgreSQL -> External Database URL
DATABASE_URL=postgresql://usuario:contraseña@host:puerto/database

# Configuración de Flask
FLASK_ENV=production
SECRET_KEY=GENERA_UNA_CLAVE_SECRETA_SEGURA_DE_AL_MENOS_32_CARACTERES

# Variables de entorno para detección de Render
RENDER=true
PRODUCTION=true

# Configuración de Email (requerido para envío de correos)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=tu_email@gmail.com
MAIL_PASSWORD=tu_password_de_aplicacion_de_gmail
MAIL_DEFAULT_SENDER=tu_email@gmail.com

# Configuración de API Keys (requeridas)
GROQ_API_KEY=gsk_tu_clave_api_de_groq_aqui
OPENAI_API_KEY=sk-tu_clave_api_de_openai_aqui

# Configuración de CORS (requerido)
# Reemplaza con tu dominio real en Render, ej: https://econova-plataforma.onrender.com
CORS_ORIGINS=https://tu-dominio-en-render.onrender.com,http://localhost:5000

# Configuración de API
API_VERSION=1.0.0

# =============================================================================
# CONFIGURACIONES OPCIONALES
# =============================================================================

# Configuración de Google Sheets (opcional - para exportación avanzada)
GOOGLE_SHEETS_CREDENTIALS_PATH=/path/to/credentials.json

# Configuración de Twilio (opcional - para SMS)
TWILIO_ACCOUNT_SID=tu_account_sid_aqui
TWILIO_AUTH_TOKEN=tu_auth_token_aqui
TWILIO_PHONE_NUMBER=+1234567890

# =============================================================================
# INSTRUCCIONES PARA CONFIGURACIÓN EN RENDER
# =============================================================================
# 1. Ve al dashboard de Render
# 2. Crea un servicio Web y conecta tu repositorio de GitHub
# 3. En Environment, agrega estas variables de entorno críticas:
#    - DATABASE_URL (desde tu base de datos PostgreSQL en Render)
#    - SECRET_KEY (genera una clave segura aleatoria)
#    - GROQ_API_KEY (desde https://console.groq.com/)
#    - OPENAI_API_KEY (opcional, desde https://platform.openai.com/)
#    - MAIL_SERVER, MAIL_USERNAME, MAIL_PASSWORD (para envío de emails)
#    - CORS_ORIGINS (tu dominio en Render)
# 4. El despliegue debería funcionar automáticamente con render.yaml
# =============================================================================
